<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/pasologo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pasologo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Passooo"
        tools:targetApi="31">
        <activity
            android:name=".loadpass"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".addpass"
            android:exported="false" />
        <activity
            android:name=".selecticon"
            android:exported="false" />
        <activity
            android:name=".enterpassword"
            android:exported="false" />
        <activity
            android:name=".createpassword"
            android:exported="false" />
        <activity
            android:name=".splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>